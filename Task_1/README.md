# Task 1: Exploring and Visualizing a Simple Dataset

Explore the **Iris dataset** by loading and inspecting it with **pandas**.  
Summarize the data using `.shape`, `.columns`, and `.head()`.  
Visualize patterns and distributions with **scatter plots, histograms, and box plots** using **matplotlib** and **seaborn**.
